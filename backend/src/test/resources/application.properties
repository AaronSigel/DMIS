spring.datasource.url=jdbc:h2:mem:dmis;MODE=PostgreSQL;DB_CLOSE_DELAY=-1
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.hibernate.ddl-auto=validate
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration/common,classpath:db/migration/{vendor}
spring.profiles.active=test

security.jwt.secret=test-secret-test-secret-test-secret-test-secret
security.jwt.expiration-seconds=3600

storage.minio.endpoint=http://localhost:9000
storage.minio.access-key=minio
storage.minio.secret-key=minio123
storage.minio.bucket=dmis-documents
